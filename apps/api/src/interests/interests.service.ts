import { Injectable } from '@nestjs/common'; import { PrismaService } from '../prisma/prisma.service'; import { InterestInput } from './dto/interest.input';
@Injectable() export class InterestsService { constructor(private readonly prisma: PrismaService) {} list(userId: string) { return this.prisma.userInterest.findMany({ where: { userId }, orderBy: { type: 'asc' } }); } async update(userId: string, inputs: InterestInput[]) { await this.prisma.$transaction(inputs.map(({ type, enabled }) => this.prisma.userInterest.upsert({ where: { userId_type: { userId, type } }, create: { userId, type, enabled }, update: { enabled } }))); return this.list(userId); } }
